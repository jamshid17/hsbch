[pytest]
# The tests import `app.*`, and the package sits next to them rather than
# being installed — this is what makes `pytest` work from the backend root
# with no PYTHONPATH dance in CI.
pythonpath = .
testpaths = tests
